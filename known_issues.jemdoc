# jemdoc: menu{MENU}{known_issues.html}
= Known issues

A more complete list of issues can be found [https://github.com/acado/acado/issues /here/]. In the following list are some /blockers/ (a bit more serious ones):

- {{<del>}}The *master* branch cannot be compiled on Windows with Visual Studio; the compiler complains about Eigen3 code (some overloaded functions). This consequently means that MATLAB interface cannot be compiled on Windows.{{</del>}}
